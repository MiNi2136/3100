import React from 'react';

const AttendanceManagementSimple = () => {
  return (
    <div style={{ padding: '20px' }}>
      <h1>Attendance Management</h1>
      <p>This is a simple test component to check if the basic component loads.</p>
      <button onClick={() => alert('Button clicked!')}>Test Button</button>
    </div>
  );
};

export default AttendanceManagementSimple;
